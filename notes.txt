NEXT TO DO:

COOL IDEAS TO ADD AT END:


pokemon cries when you choose that pokemon

add a share to twitter or ig button


WHAT DID I LEARN
html2canvas
you can use queryselectors on elements
you can access globally established variables in functions using bracket notation on window, ie: window[variable]
I thought making the dropdown would be as easy as making a select element and modifying something minor to make a search/filter bar.
that wasnt the case. Looked up how to do it, found semanticUI, but i didn't want to install a whole package just to achieve this.
I noticed the way it worked though was they had predefined classes that appeared to be triggering javascript. So I asked XpertAI about what approach I could do to achieve the same.

credits:
xpert ai dropdown css code
https://github.com/duiker101 for type icons

opengraph meta data


FETCHING IMAGE AVOIDS HOTLINKING

A Blob is a JavaScript object that holds binary file data.
So now you have:
Blob {
  type: "image/png"
  size: 48372 bytes
  data: [binary image data]
}

i split the binary data... i could skip that step in script.js 
You could simplify to:
reader.onloadend = () => {
  let imgDivs = document.querySelectorAll(`.${type}-img`);

  imgDivs.forEach((imgDiv) => {
    imgDiv.src = reader.result;
  });
};




====

NEXT TO-DO:
the new megas do exist. i need to find out how to access them, but not only access them... how to automatically access them when new ones are introduced

one thing is i am making the dropdowns manually. how to automate? need to automatically get names too...

fetch is dependent on name; we fetch by name; it cannot be automatic if i have manual names, cuz then the names wont ever be updated and thus wot exist in the app unless i make it
test: add mega victreebel

need to fetch all 18 types' arrays and THEN call dropdown
but before i do this, i want to learn branches again and work on this on a different branch

initial fetch script:

let typeURL = 'https://pokeapi.co/api/v2/type/ground';
let groundPokemon = [];
fetch(typeURL)
    // fetches data then converts to json
    .then(response => response.json())
    // use data
    .then(data => {
        for (let i = 0; i < data.pokemon.length; i++) {
            groundPokemon.push(data.pokemon[i].pokemon.name);
        }
        console.log(groundPokemon);
        console.log('count ' + groundPokemon.length);
    });
====

notes:
types endpoint: https://pokeapi.co/api/v2/type/{type}